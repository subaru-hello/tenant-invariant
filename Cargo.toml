[package]
name = "tenant-boundary-kit"
version = "0.1.0"
edition = "2021"
description = "Fail-closed tenant boundary checks for agent tool calls"
license = "MIT"
repository = "https://github.com/subaru-hello/tenant-boundary-kit"
readme = "README.md"
keywords = ["ai-agent", "multitenancy", "authorization", "security"]
categories = ["authentication", "development-tools"]

[dev-dependencies]
proptest = "1"
